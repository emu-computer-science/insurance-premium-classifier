
spring:
  profiles:
    active: dev
    
---

spring:
  config:
    activate:
      on-profile: dev
      
  flyway:
    enabled: true 
    
  datasource:
    url: jdbc:mysql://localhost:3306/insurance_premium_classifier
    username: root
    password: rootPassword360!
      
      
---

spring:
  config:
    activate:
      on-profile: test
      
  flyway:
    enabled: true
    
  datasource:
    url: jdbc:mysql://${MYSQL_HOST_TEST}/car_insurance
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}